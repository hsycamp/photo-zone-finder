
if comments
    .ui.comments
        for comment in comments
            if comment.display === true
                .comment
                    a.avatar
                        img(src='/images/avatar.png')
                    .content
                        a.author
                            | #{comment.publisher}
                        if comment.publisher === user.id 
                            span.right.floated
                                i.trash.icon
                        .text
                            | #{comment.content}